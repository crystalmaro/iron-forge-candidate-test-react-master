query DealershipInventoryActivityController($id: ID!) {
  dealership(id: $id) {
    id
    name
    address
    logoUrl
    vehicles {
     	id
    	type {
        displayName
        name
      	id
      }
      name
      address
      imageUrl
      priceCentsPerDay
    }
  }
}
